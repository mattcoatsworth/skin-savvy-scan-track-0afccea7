
/**
 * Export file for design component styles
 * Used to access component styles in a structured way
 */

import { cardVariants } from './card.styles';
import { 
  buttonColorVariants,
  buttonSizeVariants,
  buttonShadowVariants,
  buttonVariants
} from './button.styles';
import { progressVariants } from './progress.styles';
import { ratingVariants } from './rating.styles';

export {
  cardVariants,
  buttonColorVariants,
  buttonSizeVariants,
  buttonShadowVariants,
  buttonVariants,
  progressVariants,
  ratingVariants
};
